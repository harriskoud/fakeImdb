spring:
  application:
    name: auth-server
  profiles:
    active: dev
    #active: qa
    #active: prod
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888
      fail-fast: true